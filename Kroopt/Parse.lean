import Kroopt.Parse.Reader
import Kroopt.Parse.Record

/-!
# Kroopt.Parse

The pure, bounds-safe parsing and framing foundation (RFC 003). No crypto, no
FFI, no transport — a sibling of `Kroopt.Core`.
-/
